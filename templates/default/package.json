{
	"name": "@scenery/script",
	"displayName": "Script",
	"version": "0.0.1",
	"type": "module",
	"private": true,
	"engines": {
		"node": ">=16.7.0"
	},
	"scripts": {
		"dev": "cross-env NODE_ENV=development npm -s run build",
		"release": "cross-env NODE_ENV=production npm -s run build && npm -s run encrypt && npm -s run package",
		"build": "node ./bin/build.js",
		"package": "node ./bin/package.js",
		"encrypt": "node ./bin/encrypt.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"@scenery/cavalry-types": "^0.5.0",
		"adm-zip": "^0.5.12",
		"cross-env": "^7.0.3",
		"dotenv": "^16.4.5",
		"esbuild": "^0.20.2",
		"fs-extra": "^11.2.0",
		"glob": "^10.3.12",
		"node-fetch": "^3.3.2",
		"ora": "^8.0.1",
		"prettier": "^3.2.5",
		"tempy": "^3.1.0",
		"watcher": "^2.3.1"
	}
}
